[server]
host = "127.0.0.1"
port = 5317

[[server.api_tokens]]
token = "sk-test"
description = "开发测试"

[[accounts]]
email = "idyllic4202@wplacetools.com"
mobile = ""
area_code = ""
password = "test12345"

[[accounts]]
email = "espialeilani+grace@gmail.com"
mobile = ""
area_code = ""
password = "test12345"

[[accounts]]
email = "ar.r.o.g.anc.e.p.c.hz.xp@gmail.com"
mobile = ""
area_code = ""
password = "test12345"

[[accounts]]
email = "theobald2798+gladden@gmail.com"
mobile = ""
area_code = ""
password = "test12345"

[[accounts]]
email = "vj.zh.z.h.d.b.b.d.udhj.db@gmail.com"
mobile = ""
area_code = ""
password = "test12345"

[deepseek]
# 工具调用标签配置（主标签为 <|tool▁calls▁begin|> 和 <|tool▁calls▁end|>）
# 当模型输出幻觉变体时，在此追加回退标签
# tool_call.extra_starts = ["<|tool_calls_begin|>", "<|tool▁calls_begin|>", "<|tool_calls▁begin|>", "<tool_call>"]
# tool_call.extra_ends = ["<|tool_calls_end|>", "<|tool▁calls_end|>", "<|tool_calls▁end|>", "</tool_call>"]

# 已验证通过的 User-Agent
# user_agent = "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/134.0.0.0 Safari/537.36"

# API 基础地址（默认）
# api_base = "https://chat.deepseek.com/api/v0"

# WASM 文件完整 URL（PoW 计算所需，版本号会变动，建议手动更新）
# wasm_url = "https://fe-static.deepseek.com/chat/static/sha3_wasm_bg.7b9ca65ddd.wasm"

# X-Client-Version 请求头（用于 expert 模型等功能）
# client_version = "1.8.0"

# X-Client-Platform 请求头
# client_platform = "web"

# 支持的模型类型列表，默认值为 ["default", "expert"]
# 每种类型会自动映射为 OpenAI 的 model_id：deepseek-<type>
# model_types = ["default", "expert"]
# max_input_tokens = [1048576, 1048576]
# max_output_tokens = [384000, 384000]
